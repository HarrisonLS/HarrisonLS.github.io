# package.json 依赖配置

## typescript

安装 typescript 相关包

```shell

npm install typescript ts-node @types/node --save-dev

```

创建 tsconfig.json

```shell
npx tsc --init
```

[typescript.json 具体配置](/frontEnd/typescript/)
